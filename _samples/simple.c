// Type your code here, or load an example.
#include <stdio.h>
// int add(x, y ) {
//     return x + y;
// }

int main() {
    int x, y, z;
    x = 42;
    y = -5;
    z = x + y;
    // z = add(x, y)
    printf("%d + %d = %d\n", x, y, z);
    return z;
}